---
title: "The type with two elements"
subtitle: "Standard and simple types in Martin-Löf Type Theory"
description: "Disjoint union (sum) of two unit types: the type with two canonical terms. Interface"
---


```agda
{-# OPTIONS --without-K --safe #-}

module types.two where

open import types.two.core
open import types.two.decidable
```
